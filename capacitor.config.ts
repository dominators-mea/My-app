import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
 appId: 'com.joy.voidapp',
  appName: 'Void app',
  webDir: '.',
  bundledWebRuntime: false
};

export default config;